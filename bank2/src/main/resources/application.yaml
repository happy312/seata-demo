spring:
  application:
    name: bank2
server:
  port: 8082
  servlet:
    context-path: /bank2
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8080/eureka
    register-with-eureka: true
    fetch-registry: true